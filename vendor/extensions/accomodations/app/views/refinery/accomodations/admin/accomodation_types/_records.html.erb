<%= render 'refinery/admin/search_header', :url => refinery.accomodations_admin_accomodation_types_path %>
<div class='pagination_container'>
  <% if @accomodation_types.any? %>
    <%= render 'accomodation_types' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
