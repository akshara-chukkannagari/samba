<%= render 'refinery/admin/search_header', :url => refinery.accomodations_admin_accomodations_path %>
<div class='pagination_container'>
  <% if @accomodations.any? %>
    <%= render 'accomodations' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
