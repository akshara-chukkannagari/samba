<%= render 'refinery/admin/search_header', :url => refinery.regions_admin_regions_path %>
<div class='pagination_container'>
  <% if @regions.any? %>
    <%= render 'regions' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
